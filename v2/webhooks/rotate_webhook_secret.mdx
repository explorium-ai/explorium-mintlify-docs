---
title: "Rotate webhook secret"
sidebarTitle: "Rotate secret"
openapi: "post /v2/webhooks/{webhook_id}/rotate_secret"
---

Returns a new `webhook_secret` for this webhook only — others are unaffected. Replaces the v1 pattern of re-registering to rotate.
