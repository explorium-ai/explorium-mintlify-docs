---
title: "Get webhook"
sidebarTitle: "Get webhook"
openapi: "get /v2/webhooks/{webhook_id}"
---

The secret is never returned — use [Rotate webhook secret](/v2/webhooks/rotate_webhook_secret) if you lost it.
