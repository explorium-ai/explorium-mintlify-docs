---
title: "List prospect enrollments"
sidebarTitle: "List enrollments"
openapi: "get /v2/prospects/events/enrollments"
---

Each enrollment is returned with its `enrollment_id`, `webhook_id`, `status` (`active` or `paused`), `event_types`, and enrolled entity IDs.
