---
title: "Autocomplete"
sidebarTitle: "Autocomplete"
openapi: "get /v2/autocomplete"
---

Real-time suggestions for filter values.

<Note>
  **Consolidated in v2.** A single `/v2/autocomplete` endpoint replaces the separate per-entity autocomplete routes under `businesses` and `prospects`.
</Note>
